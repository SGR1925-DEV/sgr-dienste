import React from 'react';

export default function AdminPage() {
	return (
		<main className="min-h-screen flex items-center justify-center p-8">
			<div className="max-w-3xl w-full text-center">
				<h1 className="text-3xl font-bold">Admin Bereich</h1>
				<p className="mt-4 text-gray-600">
					Platzhalter-Seite für Admin-Funktionen. Diese Datei exportiert nun
					eine Default-Komponente, damit der Build-Prozess TypeScript nicht mehr
					mit "not a module" beanstandet.
				</p>
			</div>
		</main>
	);
}
